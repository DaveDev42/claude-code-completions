# Auto-generated from `claude --help` (version: 2.1.104 (Claude Code))
# fish completion for claude

complete -c claude -n "__fish_use_subcommand" -f
complete -c claude -n "__fish_use_subcommand" -a "agents" -d 'List configured agents'
complete -c claude -n "__fish_use_subcommand" -a "auth" -d 'Manage authentication'
complete -c claude -n "__fish_use_subcommand" -a "auto-mode" -d 'Inspect auto mode classifier configuration'
complete -c claude -n "__fish_use_subcommand" -a "doctor" -d 'Check the health of your Claude Code auto-updater. Note: The workspace trust dialog is skipped and stdio servers from .mcp.json are spawned for health checks. Only use this command in directories you trust.'
complete -c claude -n "__fish_use_subcommand" -a "install" -d 'Install Claude Code native build. Use [target] to specify version (stable, latest, or specific version)'
complete -c claude -n "__fish_use_subcommand" -a "mcp" -d 'Configure and manage MCP servers'
complete -c claude -n "__fish_use_subcommand" -a "plugin" -d 'Manage Claude Code plugins'
complete -c claude -n "__fish_use_subcommand" -a "plugins" -d 'Manage Claude Code plugins'
complete -c claude -n "__fish_use_subcommand" -a "setup-token" -d 'Set up a long-lived authentication token (requires Claude subscription)'
complete -c claude -n "__fish_use_subcommand" -a "update" -d 'Check for updates and install if available'
complete -c claude -n "__fish_use_subcommand" -a "upgrade" -d 'Check for updates and install if available'
complete -c claude -l add-dir -r -d 'Additional directories to allow tool access to'
complete -c claude -l agent -r -d 'Agent for the current session. Overrides the \'agent\' setting.'
complete -c claude -l agents -r -d 'JSON object defining custom agents (e.g. \'{"reviewer": {"description": "Reviews code", "prompt": "You are a code reviewer"}}\')'
complete -c claude -l allow-dangerously-skip-permissions -d 'Enable bypassing all permission checks as an option, without it being enabled by default. Recommended only for sandboxes with no internet access.'
complete -c claude -l allowedTools -r -d 'Comma or space-separated list of tool names to allow (e.g. "Bash(git:*) Edit")'
complete -c claude -l append-system-prompt -r -d 'Append a system prompt to the default system prompt'
complete -c claude -l bare -d 'Minimal mode: skip hooks, LSP, plugin sync, attribution, auto-memory, background prefetches, keychain reads, and CLAUDE.md auto-discovery. Sets CLAUDE_CODE_SIMPLE=1. Anthropic auth is strictly ANTHROP'
complete -c claude -l betas -r -d 'Beta headers to include in API requests (API key users only)'
complete -c claude -l brief -d 'Enable SendUserMessage tool for agent-to-user communication'
complete -c claude -l chrome -d 'Enable Claude in Chrome integration'
complete -c claude -s c -l continue -d 'Continue the most recent conversation in the current directory'
complete -c claude -l dangerously-skip-permissions -d 'Bypass all permission checks. Recommended only for sandboxes with no internet access.'
complete -c claude -s d -l debug -r -d 'Enable debug mode with optional category filtering (e.g., "api,hooks" or "!1p,!file")'
complete -c claude -l debug-file -r -d 'Write debug logs to a specific file path (implicitly enables debug mode)'
complete -c claude -l disable-slash-commands -d 'Disable all skills'
complete -c claude -l disallowedTools -r -d 'Comma or space-separated list of tool names to deny (e.g. "Bash(git:*) Edit")'
complete -c claude -l effort -r -a "low medium high max" -d 'Effort level for the current session (low, medium, high, max)'
complete -c claude -l exclude-dynamic-system-prompt-sections -d 'Move per-machine sections (cwd, env info, memory paths, git status) from the system prompt into the first user message. Improves cross-user prompt-cache reuse. Only applies with the default system pro'
complete -c claude -l fallback-model -r -a "sonnet opus haiku claude-sonnet-4-6 claude-opus-4-6 claude-haiku-4-5-20251001" -d 'Enable automatic fallback to specified model when default model is overloaded (only works with --print)'
complete -c claude -l file -r -d 'File resources to download at startup. Format: file_id:relative_path (e.g., --file file_abc:doc.txt file_def:img.png)'
complete -c claude -l fork-session -d 'When resuming, create a new session ID instead of reusing the original (use with --resume or --continue)'
complete -c claude -l from-pr -r -d 'Resume a session linked to a PR by PR number/URL, or open interactive picker with optional search term'
complete -c claude -s h -l help -d 'Display help for command'
complete -c claude -l ide -d 'Automatically connect to IDE on startup if exactly one valid IDE is available'
complete -c claude -l include-hook-events -d 'Include all hook lifecycle events in the output stream (only works with --output-format=stream-json)'
complete -c claude -l include-partial-messages -d 'Include partial message chunks as they arrive (only works with --print and --output-format=stream-json)'
complete -c claude -l input-format -r -a "text stream-json" -d 'Input format (only works with --print): "text" (default), or "stream-json" (realtime streaming input) (choices: "text", "stream-json")'
complete -c claude -l json-schema -r -d 'JSON Schema for structured output validation. Example: {"type":"object","properties":{"name":{"type":"string"}},"required":["name"]}'
complete -c claude -l max-budget-usd -r -d 'Maximum dollar amount to spend on API calls (only works with --print)'
complete -c claude -l mcp-config -r -d 'Load MCP servers from JSON files or strings (space-separated)'
complete -c claude -l mcp-debug -d '[DEPRECATED. Use --debug instead] Enable MCP debug mode (shows MCP server errors)'
complete -c claude -l model -r -a "sonnet opus haiku claude-sonnet-4-6 claude-opus-4-6 claude-haiku-4-5-20251001" -d 'Model for the current session. Provide an alias for the latest model (e.g. \'sonnet\' or \'opus\') or a model\'s full name (e.g. \'claude-sonnet-4-6\').'
complete -c claude -s n -l name -r -d 'Set a display name for this session (shown in /resume and terminal title)'
complete -c claude -l no-chrome -d 'Disable Claude in Chrome integration'
complete -c claude -l no-session-persistence -d 'Disable session persistence - sessions will not be saved to disk and cannot be resumed (only works with --print)'
complete -c claude -l output-format -r -a "text json stream-json" -d 'Output format (only works with --print): "text" (default), "json" (single result), or "stream-json" (realtime streaming) (choices: "text", "json", "stream-json")'
complete -c claude -l permission-mode -r -a "acceptEdits auto bypassPermissions default dontAsk plan" -d 'Permission mode to use for the session (choices: "acceptEdits", "auto", "bypassPermissions", "default", "dontAsk", "plan")'
complete -c claude -l plugin-dir -r -d 'Load plugins from a directory for this session only (repeatable: --plugin-dir A --plugin-dir B) (default: [])'
complete -c claude -s p -l print -d 'Print response and exit (useful for pipes). Note: The workspace trust dialog is skipped when Claude is run with the -p mode. Only use this flag in directories you trust.'
complete -c claude -l remote-control-session-name-prefix -r -d 'Prefix for auto-generated Remote Control session names (default: hostname)'
complete -c claude -l replay-user-messages -d 'Re-emit user messages from stdin back on stdout for acknowledgment (only works with --input-format=stream-json and --output-format=stream-json)'
complete -c claude -s r -l resume -r -d 'Resume a conversation by session ID, or open interactive picker with optional search term'
complete -c claude -l session-id -r -d 'Use a specific session ID for the conversation (must be a valid UUID)'
complete -c claude -l setting-sources -r -a "user project local" -d 'Comma-separated list of setting sources to load (user, project, local).'
complete -c claude -l settings -r -d 'Path to a settings JSON file or a JSON string to load additional settings from'
complete -c claude -l strict-mcp-config -d 'Only use MCP servers from --mcp-config, ignoring all other MCP configurations'
complete -c claude -l system-prompt -r -d 'System prompt to use for the session'
complete -c claude -l tmux -d 'Create a tmux session for the worktree (requires --worktree). Uses iTerm2 native panes when available; use --tmux=classic for traditional tmux.'
complete -c claude -l tools -r -d 'Specify the list of available tools from the built-in set. Use "" to disable all tools, "default" to use all tools, or specify tool names (e.g. "Bash,Edit,Read").'
complete -c claude -l verbose -d 'Override verbose mode setting from config'
complete -c claude -s v -l version -d 'Output the version number'
complete -c claude -s w -l worktree -r -d 'Create a new git worktree for this session (optionally specify a name)'
complete -c claude -n "__fish_seen_subcommand_from mcp" -a "serve" -d 'Start MCP server'
complete -c claude -n "__fish_seen_subcommand_from mcp" -a "add" -d 'Add an MCP server'
complete -c claude -n "__fish_seen_subcommand_from mcp" -a "remove" -d 'Remove an MCP server'
complete -c claude -n "__fish_seen_subcommand_from mcp" -a "list" -d 'List configured MCP servers'
complete -c claude -n "__fish_seen_subcommand_from mcp" -a "get" -d 'Get details of an MCP server'
complete -c claude -n "__fish_seen_subcommand_from plugin" -a "install" -d 'Install a plugin'
complete -c claude -n "__fish_seen_subcommand_from plugin" -a "uninstall" -d 'Uninstall a plugin'
complete -c claude -n "__fish_seen_subcommand_from plugin" -a "list" -d 'List installed plugins'
complete -c claude -n "__fish_seen_subcommand_from plugin" -a "update" -d 'Update plugins'
complete -c claude -n "__fish_seen_subcommand_from auth" -a "login" -d 'Log in to Claude'
complete -c claude -n "__fish_seen_subcommand_from auth" -a "logout" -d 'Log out'
complete -c claude -n "__fish_seen_subcommand_from auth" -a "status" -d 'Check authentication status'
complete -c claude -n "__fish_seen_subcommand_from install" -a "stable"
complete -c claude -n "__fish_seen_subcommand_from install" -a "latest"
